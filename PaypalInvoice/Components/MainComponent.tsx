import * as React from 'react'

interface Props {
    context : any;
}

export const MainComponent = (props: Props) => {
    //console.log(props.context);
    return (
        <div>
            hello
        </div>
    )
}
